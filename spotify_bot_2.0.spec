# -*- mode: python -*-
a = Analysis(['spotify_bot_2.0.py'],
             pathex=['C:\\Users\\spotify\\Desktop\\Spotifybot'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='spotify_bot_2.0.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='spotify_bot_2.0')
